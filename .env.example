# Optional production contact form endpoint. Leave blank to keep the form in static demo mode.
NEXT_PUBLIC_CONTACT_ENDPOINT=
# Optional map embed URL supplied by the client or hosting team.
NEXT_PUBLIC_MAP_EMBED_URL=
